import SectionHeroPM from "@/components/Curriculum/PM/SectionHeroPM";

//교육과정 > PM
const page = () => {
  return(
    <main>
      {/* 상단 Hero 세션 - KDT신청하기 버튼 */}
      <SectionHeroPM/>
    </main>
  )
}

export default page;